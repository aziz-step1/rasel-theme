
<section class="py-10 px-4 bg-gray-50 text-center">
  <h2 class="text-2xl font-bold mb-4">روابط دائرية</h2>
  <div class="flex flex-wrap justify-center gap-4">
    {% for i in (1..6) %}
      <div class="w-20 h-20 bg-gray-200 rounded-full flex items-center justify-center text-gray-500">تصنيف</div>
    {% endfor %}
  </div>
</section>
