<div class="rasel-product-card bg-white rounded-xl shadow p-4 text-center">
  <img src="{{ product.image }}" alt="{{ product.name }}" class="mx-auto mb-2 rounded-lg w-full h-auto" />
  <h3 class="text-lg font-bold mb-1">{{ product.name }}</h3>
  <p class="text-primary font-semibold">{{ product.price | money }}</p>
</div>